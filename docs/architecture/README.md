# PeakCloud Architecture

PeakCloud is designed as a modular cloud storage platform.

The initial platform consists of:

- Next.js web application
- Go HTTP API
- PostgreSQL metadata database
- Redis cache
- S3-compatible object storage
- Background worker architecture

Architecture documentation will evolve alongside the platform as additional storage, synchronization, sharing, security, and distributed-system capabilities are introduced.
